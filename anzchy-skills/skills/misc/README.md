# Misc

Kept around, rarely used, not promoted. These are **not** shipped in the plugin and are absent from `plugin.json`. Install one directly if you want it.

- **[songy-course-exporter](./songy-course-exporter/SKILL.md)**: Export one `webapp.songy.info` course into a folder holding one merged MP3 and one Markdown notes file.
